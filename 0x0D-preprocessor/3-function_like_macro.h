#ifndef MACROLIKE_H
#define MACROLIKE_H

/**
 * file: 3-function_like_macro.h
 * function-like macro ABS(x) that computes
 * the absolute value of a number of x
 * @x: parameter
 * Description: header file
 */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
